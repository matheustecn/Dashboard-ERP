services:
  - type: web
    name: guapo-erpweb
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn guapo_erp:server --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
